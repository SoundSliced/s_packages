name: s_liquid_pull_to_refresh_example
publish_to: 'none'
description: Example app demonstrating s_liquid_pull_to_refresh usage
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  s_liquid_pull_to_refresh:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
