# SOffstage Example

This example demonstrates how to use the `s_offstage` package inside a Flutter app.

To run the example locally:

1. Ensure you have a Flutter SDK installed and setup.
2. From this repository root run:

```bash
cd example
flutter pub get
flutter run
```

The example app lives in `example/lib/main.dart`.
