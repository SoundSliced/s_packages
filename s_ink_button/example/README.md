# Example app for s_ink_button

This example demonstrates basic and advanced usage of `s_ink_button`.

Run the example with:

```bash
cd example
flutter run
```

Make sure you run `flutter pub get` in the project root and in the example directory if needed.