# s_expendable_menu example

This example demonstrates both basic and advanced usage of `s_expendable_menu`.

## What’s inside

- **Basic menu** showing the default look and behavior.
- **Advanced menu** with toggles for size, colors, RTL layout, and item count.
- **Handles demo** to showcase `SExpandableHandles` advanced animations.

## Run it

```bash
cd example
flutter pub get
flutter run
```
